import { world } from "./world";

/**
 * TODO:
 * explicit anchor
 * smooth transition
 * elevation tile -1
 * mercator elevation
 * subdivide const
 */

world(document.querySelector("canvas") as HTMLCanvasElement);
